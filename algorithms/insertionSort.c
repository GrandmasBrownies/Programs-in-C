#include <stdio.h>

int main()
{
    int array[7] = {1,5,8,3,0,7,4};
    int size = 7;

    for (int i = 1; i < size-1; i++)
    {
        if (array[i] < array[i-1]) {
            // start while loop and count backwards 
        }
    }
}